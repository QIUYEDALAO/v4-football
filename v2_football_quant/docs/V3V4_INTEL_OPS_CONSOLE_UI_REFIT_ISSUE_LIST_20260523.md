# V3/V4 Intel Ops Console UI Refit Issue List - 2026-05-23

1. 当前 UI 与最终冻结版不一致。
2. V2 full purge 后，页面仍显示 “V2 active”，说明 checker false pass。
3. 当前候选区只有 A/B/C 数量，缺少可折叠候选卡片体验。
4. V3 没有正式展示区或预留窗口。
5. 系统安全区仍保留 V2 文案。
6. daily refresh 必须输出 V3/V4 only dashboard。
7. HTTP 访问恢复后必须复测 live HTML，而不是只扫文件。
8. checker 必须扫描实际 served HTML。
9. V2 只能出现在 purge 报告中，不能出现在 active dashboard。
10. V33 不能作为 V3 出现。

Status: PASS
